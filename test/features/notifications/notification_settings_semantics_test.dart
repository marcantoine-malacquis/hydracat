import 'package:flutter_test/flutter_test.dart';

void main() {
  group('NotificationSettingsScreen semantics', () {
    testWidgets(
      'placeholder (semantics covered in integration tests)',
      (tester) async {},
      skip: true,
    );
  });
}
