import 'package:flutter/material.dart';

/// Constants for all app colors used throughout the HydraCat application.
class AppColors {
  // Primary colors
  /// Primary blue color for the app theme
  static const Color primary = Color(0xFF2196F3); // Blue
  /// Primary variant color, darker shade of primary
  static const Color primaryVariant = Color(0xFF1976D2);

  /// Secondary green color representing health
  static const Color secondary = Color(0xFF4CAF50); // Green for health

  // Semantic colors
  /// Success color for positive actions
  static const Color success = Color(0xFF4CAF50);

  /// Warning color for cautionary actions
  static const Color warning = Color(0xFFFF9800);

  /// Error color for negative actions
  static const Color error = Color(0xFFF44336);

  /// Information color for informational content
  static const Color info = Color(0xFF2196F3);

  // Neutral colors
  /// Background color for the app
  static const Color background = Color(0xFFFAFAFA);

  /// Surface color for cards and containers
  static const Color surface = Color(0xFFFFFFFF);

  /// Text color on primary background
  static const Color onPrimary = Color(0xFFFFFFFF);

  /// Text color on secondary background
  static const Color onSecondary = Color(0xFFFFFFFF);

  /// Text color on background
  static const Color onBackground = Color(0xFF212121);

  /// Text color on surface
  static const Color onSurface = Color(0xFF212121);

  // Custom colors for HydraCat
  /// Blue color specifically for hydration elements
  static const Color hydrationBlue = Color(0xFF03A9F4);

  /// Green color representing kidney health
  static const Color kidneyHealth = Color(0xFF8BC34A);

  /// Blue color representing water drops
  static const Color waterDrop = Color(0xFF00BCD4);

  // Dark theme variants
  /// Dark theme background color
  static const Color darkBackground = Color(0xFF121212);

  /// Dark theme surface color
  static const Color darkSurface = Color(0xFF1E1E1E);

  /// Text color on dark background
  static const Color darkOnBackground = Color(0xFFE0E0E0);

  /// Text color on dark surface
  static const Color darkOnSurface = Color(0xFFE0E0E0);
}
